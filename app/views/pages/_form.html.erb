
<h3>Type in your meter results.</h3>

<%= form_for Entry.new do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :results %>
  </div>
  <div class="actions">
    <%= f.submit "Add card" %>
  </div>
  <span class="md f"><p>mg/dl</p></span>
<% end %>
